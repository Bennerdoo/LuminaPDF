# Example Docker Compose for Unified Lumina-PDF Container
# MODE=FRONTEND: Frontend only, connects to separate backend

services:
  Lumina-PDF-backend:
    container_name: Lumina-PDF-Backend
    build:
      context: ../..
      dockerfile: docker/Dockerfile.unified
    ports:
      - "8081:8080"
    volumes:
      - ./lumina/data:/usr/share/tessdata:rw
      - ./lumina/config:/configs:rw
      - ./lumina/logs:/logs:rw
      - ./lumina/customFiles:/customFiles:rw
      - ./lumina/pipeline:/pipeline:rw
    environment:
      MODE: BACKEND
      DISABLE_ADDITIONAL_FEATURES: "false"
      DOCKER_ENABLE_SECURITY: "false"
      PUID: 1000
      PGID: 1000
      UMASK: "022"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080$${SYSTEM_ROOTURIPATH:-''}/api/v1/info/status || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 4G

  Lumina-PDF-frontend:
    container_name: Lumina-PDF-Frontend
    build:
      context: ../..
      dockerfile: docker/Dockerfile.unified
    ports:
      - "8080:8080"
    environment:
      MODE: FRONTEND

      # Point to the backend service
      VITE_API_BASE_URL: http://Lumina-PDF-backend:8080

      # Minimal config needed for frontend
      PUID: 1000
      PGID: 1000
    depends_on:
      - Lumina-PDF-backend
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M
